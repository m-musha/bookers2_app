
<div class="container">
  <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">

<% if @book.errors.any? %>
    <div id="error_explanation">
      <h3 class="text-danger"><%= pluralize(@book.errors.count, "error") %> prohibited this obj from being saved:</h3>
      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li class="text-danger"><%= message %></li>
        <% end %>
      </ul>
    </div>
<% end %>

      
<h2>Editing Book</h2>
<%= form_with model:@book, local: true do |f| %>

<div class="form-group">
  <label for="inputbook">title</label>
  <%= f.text_field :title, class: "form-control title" %></div>
  
<div class="form-group">
  <label for="inputbook">Opinion</label>
  <%= f.text_area :body, class: "form-control opinion" %></div>
  
<div class="form-group">
  <%= f.submit "Update Book", class: "btn btn-success" %></div>
  
  <% end %>
  <%= link_to "Show", book_path(@book) %> |
  <%= link_to "Back", books_path(@books) %>

